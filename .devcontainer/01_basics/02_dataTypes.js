"use strict"; // treat all JS code as newer version  จะเขียนด้วย JS code แบบสมัยใหม่

// alert(3+3); // we are using nodejs, not browser

console.log(3 + 3); // code readability should be high 

console.log("Rockmafia");


let name = "Rock"
let age = 20
let isLoggedIn = true
let state = null


// number => 2 to power 53 
// bigint
// srting => "" ใช้ Double "" ดีกว่า
// boolean => true/flase
// null => standalone value  สมมุติ server ส่งค่าสภาพอากาศมา ถ้าส่งเป็น 0 ก็ไม่ได้ เพราะ 0 ก็เป็นสภาพอากาศเหมือนกัน เพราะฉะนั้นเขาเลยใช้ Null ในการส่งค่าว่าง 
// undifined => 
// symbol => unique


// object 

console.log(typeof undefined); // undifined
console.log(typeof null); //null เป็น Object 